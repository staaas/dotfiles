[user]
  name = Stas Rudakou
  email = stas@garage22.net
  signingKey = {{ ansible_env.HOME }}/.ssh/id_ecdsa_sk.pub
[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  hist = log --pretty=format:\"%h %ad | %s%d [%an <%ae>]\" --graph --date=short
  type = cat-file -t
  dump = cat-file -p
[color]
  ui = true
[gpg]
  format = ssh
