(:name auth-password-store
       :type github
       :description "Integrate Emacs' auth-source with password-store"
       :pkgname "DamienCassou/auth-password-store"
       :depends (seq password-store)
       :postinit (progn (require auth-password-store)
                        (auth-pass-enable)))
